apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong
  template:
    metadata:
      labels:
        app: pingpong
    spec:
      volumes:
        - name: shared-storage
          persistentVolumeClaim:
            claimName: shared-data-pvc
      containers:
        - name: pingpong
          image: ping-pong:1.1
          imagePullPolicy: Never
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: shared-storage
              mountPath: /usr/src/app/files
