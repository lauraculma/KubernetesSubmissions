apiVersion: v1
kind: Service
metadata:
  name: project-postgres-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: project-postgres
  ports:
    - port: 5432
      targetPort: 5432
